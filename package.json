{
  "name": "creator-gacha",
  "private": true,
  "type": "module",
  "description": "Dev harness only \u2014 the shipped app is plain ES modules with zero dependencies and no build step. Vitest runs the test suite against the pure core. `dev` serves the site with the legacy KV lobby in one terminal; to exercise the Durable Object lobby run `dev:room` and `dev:lobby` in two.",
  "scripts": {
    "dev": "npx wrangler pages dev . --kv READY --compatibility-date=2026-08-15",
    "dev:static": "npx serve",
    "dev:room": "npx wrangler dev --config workers/match-room/wrangler.jsonc",
    "dev:lobby": "npx wrangler pages dev . --do ROOM=MatchRoom@creator-gacha-room --compatibility-date=2026-08-15",
    "deploy:room": "npx wrangler deploy --config workers/match-room/wrangler.jsonc",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:report": "node tools/test-report.js",
    "build:site": "node tools/build-site.js",
    "status": "node tools/status.js",
    "runs": "node tools/refresh-runs.js",
    "deploy": "node tools/build-set.js && node tools/build-site.js && npx wrangler pages deploy _site --project-name=creator-gacha --branch=main && node tools/record-deploy.js",
    "deploy:filtered": "node tools/build-set.js --apply-staged && node tools/build-site.js && npx wrangler pages deploy _site --project-name=creator-gacha --branch=main && node tools/record-deploy.js"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
