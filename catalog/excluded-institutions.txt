# ══════════════════════════════════════════════════════════════════════════
# STAGED — NOT APPLIED TO THE LIVE DECK.
#
# This file is a curation decision UNDER REVIEW. tools/build-set.js reads it to
# work out what a filtered printing WOULD look like, and ships nothing on the
# strength of it. The live deck is built from catalog/excluded.txt alone.
#
# To apply it once review is finished:
#     node tools/build-set.js --apply-staged     (or npm run deploy:filtered)
#
# WHY THIS IS A SEPARATE FILE, and it is a mistake being corrected rather than a
# feature. Institution ids were once appended straight into catalog/excluded.txt,
# which is the SETTLED list the build always applies — so an in-progress
# judgement went live the moment it was written, and 42% of the deck disappeared
# from a public site before anybody had reviewed a line of it. A pending decision
# must not be able to reach production by default. Two files, two states:
#
#     catalog/excluded.txt                settled — always applied
#     catalog/excluded-institutions.txt   staged  — applied on request
#
# CURRENTLY EMPTY. The 7,705 ids staged here as of 2026-08-04 were promoted to
# catalog/excluded.txt whole, on Ash's call (2026-08-05) — "remove them, they
# don't add anything, nobody will care" — closing that review rather than
# working through it line by line. The mechanism stays: the next institution
# sweep, or any future bulk curation call, stages here first and is promoted
# the same way once decided.
#
# A second batch (18 ids, corporations and broadcasters caught by a name-only
# scan of the 2026-08-16 sourcing pass) sat here briefly and was reviewed and
# DECLINED the same day — Ash's call: keep them rather than chase a handful of
# cards out of a 20k+ deck. Removed rather than left as dead weight; the ids
# and the reasoning for flagging each one are in git history if this ever
# needs revisiting.
